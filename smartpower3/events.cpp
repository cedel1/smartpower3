#include "events.h"
//#include "screen.h"

//esp_event_loop_handle_t loop_with_task;

//void settings_voltage0_changed_handler (void *handler_args, esp_event_base_t base, int32_t id, void *event_data)
//{
//	screen->getChannel(0)->setVolt(settings->getChannel0Voltage(), 1);
//}
